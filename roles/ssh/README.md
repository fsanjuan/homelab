# Role: ssh

Ensures the SSH server is enabled and running on boot.

## What it does

- Enables the `ssh` service so it starts automatically after a reboot
- Starts the service if it is not currently running
